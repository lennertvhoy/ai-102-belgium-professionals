import React, { useState } from 'react';
import styles from './styles.module.css';

export default function QuizPoc() {
  return (
    <div className={styles.quizContainer}>
      <h2>Proof-of-Concept Quiz</h2>
      <p>This is a placeholder for the interactive quiz component.</p>
      <p>The purpose of this POC is to validate the 'Islands Architecture' approach.</p>
    </div>
  );
}
